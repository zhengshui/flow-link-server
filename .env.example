# ============================================
# 环境配置示例文件
# 复制此文件为 .env 并填入实际值
# cp .env.example .env
# ============================================

# 应用环境: development | production
APP_ENV=development

# 服务器配置
SERVER_ADDRESS=0.0.0.0:8080
PORT=8080
CONTEXT_TIMEOUT=30

# 数据库配置
# 开发环境使用 localhost，生产环境使用 mongodb（Docker 服务名）
DB_HOST=localhost
DB_PORT=27017
DB_USER=admin
DB_PASS=your_secure_password_here
DB_NAME=flow_link

# JWT 配置
# 警告: 生产环境必须使用强随机密钥！
# 生成方式: openssl rand -hex 32
ACCESS_TOKEN_EXPIRY_HOUR=24
REFRESH_TOKEN_EXPIRY_HOUR=168
ACCESS_TOKEN_SECRET=your_access_token_secret_change_in_production
REFRESH_TOKEN_SECRET=your_refresh_token_secret_change_in_production

# ============================================
# 生产环境专用配置（仅 docker-compose.prod.yaml 使用）
# ============================================

# Docker 镜像仓库（可选，用于推送镜像）
# DOCKER_REGISTRY=your-registry.com/

# 镜像版本标签
# VERSION=latest
